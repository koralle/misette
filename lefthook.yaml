pre-commit:
  piped: true
  jobs:
    - name: Fix with Ultracite
      glob:
        - "**/*.js"
        - "**/*.cjs"
        - "**/*.mjs"
        - "**/*.jsx"
        - "**/*.ts"
        - "**/*.cts"
        - "**/*.mts"
        - "**/*.tsx"
        - "**/*.json"
        - "**/*.jsonc"
        - "**/*.css"
        - "**/*.scss"
        - "**/*.md"
        - "**/*.mdx"
        - "**/*.yaml"
        - "**/*.yml"
        - "**/*.html"
      run: |
        pnpm run fix -- {staged_files}
      stage_fixed: true

    - group:
        parallel: true
        jobs:
          - name: Typecheck
            glob:
              - "**/*.{ts,tsx}"
            run: |
              pnpm run typecheck

pre-push:
  commands:
    betterleaks:
      run: betterleaks git . --no-banner -v
